// Twitter card image — reuses the same generator and metadata as the
// Open Graph image so the two stay identical. Next.js emits the
// twitter:image tags from this file convention.
export { default, size, contentType, alt } from "./opengraph-image";
